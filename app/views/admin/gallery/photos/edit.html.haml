.page-header
	= link_to "Return to #{@gallery.title}", admin_gallery_gallery_photos_path(@gallery)
	%h2 Editing Photo

= comfy_gallery_form_for @photo, :url => { :action => :update }, :html => { :multipart => true } do |form|
	= render form
