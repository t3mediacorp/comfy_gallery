.page-header
	= link_to "Back to #{@gallery.title}", admin_gallery_gallery_photos_path(@gallery)
	%h2 Editing Photo

= comfy_gallery_form_for @photo, :url => { :action => :update }, :html => { :multipart => true } do |form|
	= render form

- content_for :head do
	=stylesheet_link_tag 'comfy_gallery/gallery'
